import { Listings } from "@/components/dashboard/Listings";

export const metadata = {
  title: "Listings | Dashboard",
  description: "Manage your institution branches and courses",
};

export default function ListingsPage() {
  return <Listings />;
}